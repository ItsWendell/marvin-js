// import scheduler from '../../providers/scheduler';
// import { sendCoalitionStats, fetchCoalitionStats, coalitionInterval } from './intra42';

export function register() {
  // Send daily updates to general channel with coalition updates
  // scheduler.scheduleJob('0 10 * * *', () => {
  //   console.log('[Jobs] Sending Coalition Stats...');
  //   sendCoalitionStats();
  // });
  // scheduler.scheduleJob(`*/${coalitionInterval} * * * *`, () => {
  //   console.log('[Jobs] Fetching coalition updates...');
  //   fetchCoalitionStats();
  // });
}
